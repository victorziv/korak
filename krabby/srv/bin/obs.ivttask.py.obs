#!/usr/bin/env python
from config import Configurator
Configurator.configure()
Configurator.set_logging("trackivtsrv", console_logging=True)
from config import conf, logger  # noqa
# ______________________________


def main():
    task = 'fcloopback'
    run_task(task)
# ______________________________


def run_task(task):
    from srv.task.fcloopback import TaskFcloopback
    task = TaskFcloopback()
    task.run()
# ______________________________


if __name__ == '__main__':
    main()
